#include <stdio.h>

int main(){
    int base=9,height=12,length,area;
    printf("Insert the value of length \n");
    scanf("%d",& length);
      area= length*base*height;
  printf("The area of rectangle is %d",area);
    return 0;
}